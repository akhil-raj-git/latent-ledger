# Optional local overrides. GitHub Actions uses only _config.yml.
#
# Serve at http://127.0.0.1:4000/ (no /latent-ledger prefix):
#   bundle exec jekyll serve --livereload --config _config.yml,_config_dev.yml
#
# Default `jekyll serve` uses _config.yml only — same URL layout as production.

url: "http://127.0.0.1:4000"
baseurl: ""
